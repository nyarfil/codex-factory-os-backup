# AI-CAD（必要時の参照）

factory-cad-3dpがAI-CAD指定・既存AI-CAD案件で使う。既存Fusion/ForgeCAD/cadgen正本を切り替える指示ではない。

旧導入候補は `E:/aiwork/Stella_CAD_SYSTEM/ai-cad-labs`。まず実在・現在のAGENTS・Python環境・プロジェクト配置を調べ、見つかった現行手順を使う。見つからなければ利用不可と報告し、別案件のパスを推測しない。

旧運用にあった `V:/mouse/.cursor/cad-boot.ps1` の自動実行、`.cursor/cad-session.json`の書換え、固定projectsへの出力は新しい案件では行わない。必要ならその案件の現行手順として明示的に確認する。サブエージェント・モデル・予算はFactory OSが管理する。

この参照追加ではAI-CADランタイムを起動・インストールしていない。
