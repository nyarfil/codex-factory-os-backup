# Implementation
Map the smallest execution path, define invariants and acceptance tests, assign one writer for overlapping files, run targeted checks first, and broaden verification with blast radius.
