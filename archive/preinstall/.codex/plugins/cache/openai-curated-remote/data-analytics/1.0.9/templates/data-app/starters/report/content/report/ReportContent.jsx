// Compose the report's answer and reviewed evidence with the public components.
// Resolve current component docs through AGENTS.md, then read the reports topic.
// This empty starter is scaffolding, not a completed report.
export function ReportContent() { return null; }
