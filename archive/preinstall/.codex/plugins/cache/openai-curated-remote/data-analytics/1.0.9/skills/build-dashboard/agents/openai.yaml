interface:
  display_name: "Build Dashboard"
  short_description: "Create source-backed dashboards for monitoring performance, exploring drivers, and acting on metrics."
  default_prompt: "Build the shared Data app as a dashboard with reviewed data, useful filters, and working KPI and chart menus; use BI tools as data sources unless the user explicitly chooses a BI dashboard destination."
policy:
  allow_implicit_invocation: true
